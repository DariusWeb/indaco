{
    "name": "nuxt-legal-backend",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "json-server --watch db.json --port 3001 --delay 150",
        "start": "json-server --watch db.json --port 3001"
    },
    "dependencies": {
        "json-server": "^0.17.1"
    }
}